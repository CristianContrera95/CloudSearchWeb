
const config = {
    // URL_BACKEND: window.location.protocol + '//' + window.location.hostname + ':5000/api/',
    URL_BACKEND: 'https://searchjuba-api.azurewebsites.net/api/'
}

export default config;
